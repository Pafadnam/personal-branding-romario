[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Redirection propre vers l'admin (évite les 404 sur /admin sans slash)
[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301
